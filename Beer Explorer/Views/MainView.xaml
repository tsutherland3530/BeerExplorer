<Window x:Class="BeerExplorer.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Beer Explorer" Height="450" Width="800">
	<Grid>
		<TextBox Text="{Binding SearchQuery}" Width="300" Margin="10" />
		<Button Content="Search" Command="{Binding SearchCommand}" Width="100" Margin="10" />
		<ListBox ItemsSource="{Binding BeerStyles}" DisplayMemberPath="Name" Margin="10,50,10,10" />
	</Grid>
</Window>
